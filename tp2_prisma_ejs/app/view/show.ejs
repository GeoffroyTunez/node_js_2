<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detail utilisateur</title>
    <style>
        body {
            font-family: Arial, sans-serif;
        }
        form {
            max-width: 400px;
            margin: auto;
        }
        label {
            display: block;
            margin: 10px 0 5px;
        }
        input, a {
            width: 100%;
            padding: 8px;
            margin: 5px 0 20px;
            border: 1px solid #ddd;
            border-radius: 4px;
        }
        a {
            background-color: #28a745;
            color: white;
            border: none;
            cursor: pointer;
            text-decoration: none;
        }
    </style>
</head>
<body>
    <h1>Modifier un utilisateur</h1>
    <form id="editUserForm">
        <label for="id">Id :</label>
        <input type="text" id="id" name="id" value="<%= user.id %>"  readonly="readonly" required />

        <label for="name">Nom :</label>
        <input type="text" id="name" name="name" value="<%= user.name %>"  readonly="readonly" required />

        <label for="age">Âge :</label>
        <input type="number" id="age" name="age" value="<%= user.age %>"  readonly="readonly" required />
        <a href="/view/users">Retour</a>
    </form>

    
</body>
</html>
